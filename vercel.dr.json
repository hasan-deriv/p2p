{
    "cleanUrls": true,
    "outputDirectory": "dist",
    "buildCommand": "echo ✅  Skipping build to use existing built files",
    "routes": [
        { "handle": "filesystem" },
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}
